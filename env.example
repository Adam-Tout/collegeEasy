# API Configuration
# For AWS deployment, set this to your API Gateway endpoint
# Example: https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/prod/chat
# Leave empty to use demo mode (no backend needed)
VITE_API_URL=
VITE_APP_NAME=Canvas AI Assistant
VITE_APP_VERSION=1.0.0

# Backend API Configuration
# Set to 'false' to disable backend API and use demo mode
# For AWS deployment, set to 'true' and configure VITE_API_URL with your Lambda endpoint
# Default: false (uses demo mode for static hosting)
VITE_USE_BACKEND_API=false

# Note: For AWS Lambda backend, you'll need to set up:
# - Lambda function for OpenAI API calls
# - API Gateway endpoint
# - Set OPENAI_API_KEY in Lambda environment variables
# See AWS_DEPLOYMENT.md for detailed instructions

# Google OAuth (Optional)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Payment Processing (Stripe)
VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# Canvas API Configuration
VITE_CANVAS_API_URL=https://your-canvas-instance.instructure.com/api/v1

# Development
VITE_DEBUG=false
VITE_NODE_ENV=development
